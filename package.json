{
  "name": "easymongo",
  "version": "6.0.1",
  "description": "Thin, opinionated wrapper around the MongoDB Node.js driver with a fail-silent promise API.",
  "keywords": [
    "easymongo",
    "mongo",
    "mongodb",
    "promise"
  ],
  "license": "MIT",
  "author": {
    "name": "Alexey Simonenko",
    "email": "alexey@simonenko.dev",
    "url": "http://simonenko.xyz"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meritt/easymongo.git"
  },
  "files": [
    "lib"
  ],
  "type": "module",
  "exports": "./lib/index.js",
  "scripts": {
    "lint": "oxlint",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "test": "node --test test/*.js",
    "coverage": "mkdir -p coverage && node --test --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info test/*.js"
  },
  "dependencies": {
    "mongodb": "^7.2"
  },
  "devDependencies": {
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0"
  },
  "engines": {
    "node": ">=24.14"
  }
}
